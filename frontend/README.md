# Citation Check Frontend

React + Vite interface for the course-demo citation checker.

```bash
npm install
npm run dev
```

The UI is intentionally small: one demo sample, one audit button, inline sentence highlights, and a sentence-level report.
